[Unit]
Description=Run OpenClaw usage cache update periodically

[Timer]
OnBootSec=1min
# Run frequently, but the updater is "smart" and will no-op unless cache is stale
# for the current activity state.
OnUnitActiveSec=2min
AccuracySec=30s
Persistent=true

[Install]
WantedBy=timers.target
